node_modules/
dist/
.env
.env.*
!.env.example
# Any *.env file — e.g. .migrate.env, a local-only file holding the production
# DATABASE_URL for one-off migrations. Never commit a live credential.
*.env
.migrate.env
# Local-only dumps of production tables taken before a manual data fix.
.prod-backup/
*.log
.DS_Store
coverage/
uploads/
